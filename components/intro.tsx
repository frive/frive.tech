import { SITE_NAME } from '../lib/constants';

const Intro = () => {
  return (
    <section className="flex-col md:flex-row flex items-center md:justify-between mb-8 md:mb-4">
      <h4 className="text-center md:text-left text-xl mt-5">A tech blog.</h4>
    </section>
  );
};

export default Intro;
